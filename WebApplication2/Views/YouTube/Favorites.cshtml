@model List<WebApplication2.Models.Video>

@{
    ViewData["Title"] = "Ulubione wideo";
}

<!-- Nagłówek strony -->
<h2>⭐ Ulubione wideo</h2>

<!-- Jeśli nie ma zapisanych filmów -->
@if (Model.Count == 0)
{
    <p class="text-center">❌ Brak zapisanych filmów.</p>
}
else
{
    <!-- Lista zapisanych filmów -->
    <div class="container">
        <div class="row">
            @foreach (var video in Model)
            {
                <div class="col-md-4 mb-4">
                    <div class="card">
                        <!-- Podgląd filmu z YouTube -->
                        <iframe class="card-img-top" width="100%" height="200" 
                                src="https://www.youtube.com/embed/@video.VideoId" 
                                frameborder="0" allowfullscreen>
                        </iframe>

                        <div class="card-body text-center">
                            <h6 class="card-title">@video.Title</h6>

                            <!-- Formularz do usunięcia filmu z ulubionych -->
                            <form asp-controller="YouTube" asp-action="RemoveFromFavorites" method="post">
                                <input type="hidden" name="videoId" value="@video.VideoId" />
                                <button type="submit" class="btn btn-danger mt-2">❌ Usuń</button>
                            </form>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
}



